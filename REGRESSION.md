# 回归清单（发版前人工走查）

对应版本：v0.6.7 ｜ 更新日期：2026-08-21

`tools/verify.js` + 两套离线测试覆盖的是构建一致性、服务层逻辑与解析容错；**真实 Discord 客户端里的注入、弹窗交互和不可逆删除，只能人工走查**。本清单就是那部分。改动涉及哪几节就至少跑那几节，涉及删除链路（14a / 19 区）必须跑完第 4 节。

自动化前置（必须全绿，不绿不用往下走）：

```bash
node tools/build.js && node tools/verify.js && node tools/smoke_test.js && node tools/test_harness.js
```

部署：把根目录 `DiscordAIMessageCleaner.plugin.js` 拷到 BD 的 `plugins` 目录，BD 会热重载；控制台（Ctrl+Shift+I）保持打开，全程不应出现本插件的红色报错。

## 0. 准备一个安全的实验场

删除不可逆，**永远不要拿真实聊天记录做第一次验证**。

1. 自己新建一个测试服务器，建两个文本频道（用于验证跨频道路由）。
2. 在两个频道各发 6-8 条消息，内容混合：纯文本、带图片附件、纯附件无文本、编辑过的、超长文本、含自定义表情。
3. 另在一个 DM（可与自己的另一个小号或任意好友）发 3-5 条消息，用于验证 DM 分支。
4. 设置 → 审查策略：临时建一条"命中一切"的策略（例如"把所有消息都判为违规"），这样不依赖 AI 判得准，就能把删除链路走满。

## 1. 入口与环境

- [ ] 服务器文本频道：聊天输入框出现插件图标，tooltip 正确；点击打开弹窗。
- [ ] 频道右键菜单、DM 列表右键菜单出现「AI 消息清理」并能打开。
- [ ] `/aiclean` 斜杠命令能打开弹窗。
- [ ] DM 与群聊里按钮同样出现（与总结插件不同，此处刻意放开）。
- [ ] 语音频道 / 不支持的位置：显示「不支持的频道」提示而非报错。
- [ ] 设置 → 诊断页：入口状态为 `webpack`，`rest`/`chatButtons`/`chatButtonChrome`/`modalSystem` 与六个 store 全部「正常」；「复制诊断信息」能贴出 JSON。
- [ ] 切换 zh-CN / en-US：弹窗与设置页无残留中文/英文，无 `undefined`。

## 2. 扫描

- [ ] 当前频道 + 7 天：进度条推进，条数与实际相符，只列出自己的消息。
- [ ] 整个服务器：结果跨两个频道，每行带 `#频道名` 徽章；频道下拉筛选可用。
- [ ] 时间范围自定义（含"全部"）：结果不超出所选区间；「全部」按上限从最新往回。
- [ ] 扫描中点取消：立即停止，已扫部分仍可用。
- [ ] 扫描中点背板/Esc 关掉弹窗，再打开：结果由 ScanCache 恢复，不用重扫。
- [ ] 命中上限（把 `fetch.maxMessages` 临时调到 10）：出现截断提示 + 「继续扫描更早的消息」，续扫结果合并且无重复行。
- [ ] DM / 群聊：走逐页扫描（无搜索），结果正确。

## 3. AI 审查

- [ ] 未配置 AI：横幅提示，仍可手动勾选，主按钮不假死。
- [ ] 配好平台后审查：进度按批推进，命中项高亮 + 自动勾选，理由可读。
- [ ] 「后台运行」：关闭弹窗后悬浮胶囊出现在聊天输入框上方、右缘对齐，不遮挡翻译插件胶囊；进度持续更新；点胶囊回到弹窗，结果完整。
- [ ] 后台运行期间切频道 / 改窗口大小：胶囊重新锚定，不跑到窗口角落。
- [ ] 断网或填错 API Key：整批进重试桶，出现「部分失败」+ 重试按钮；**没有任何消息被标为违规**。
- [ ] token 门（`review.confirmAboveTokens` 调小）：出现超额确认，取消后不发请求。

## 4. 删除（核心，务必在测试服务器里做）

安全性三连——这三条任何一条不成立，立刻停止发版：

- [ ] 点「删除选中」弹出的危险确认里，点**取消**：什么都没被删除。
- [ ] 同一确认弹窗，按 **Esc**：什么都没被删除。
- [ ] 同一确认弹窗，点**背板**（弹窗外区域）：什么都没被删除。

备份与执行：

- [ ] 确认弹窗正文显示待删条数；勾选框初始状态符合设置（`ask`/`never` 可点，`always` 锁定为勾选并显示"按设置"文案）；勾选后显示 Markdown / TXT / JSON 格式选择。
- [ ] 分别选择 MD、TXT、JSON → 确认：先弹系统保存对话框，保存后才开始删除；三种文件的条数、正文、附件名/链接均对得上。
- [ ] 备份保存对话框点取消：**删除不执行**，提示"已取消"。
- [ ] 不勾选备份 → 确认：直接进入删除，节流可见（默认 1200ms/条，进度逐条推进）。
- [ ] Discord 里对应消息真的消失了（刷新频道确认）。
- [ ] 服务器范围的删除：两个频道的消息都被正确删除（逐条按自己的 channelId 路由）。
- [ ] 超过单次上限（把 `delete.maxPerRun` 调到 3，选 6 条）：确认弹窗提示只删最新 3 条，实际也只删 3 条，其余仍在列表里。

中断与异常：

- [ ] 删除中点「暂停」：进度停住；「继续」后从下一条接着删。
- [ ] 删除中点「取消」：立即停止，进入报告页并标注"已取消"，已删条数与实际一致。
- [ ] 删除中直接关闭弹窗：删除**停止**（不在后台偷偷删）；重开弹窗后列表里已删的消息不再出现。
- [ ] 报告页：成功/跳过/失败数字正确；有失败项时列出 id 与 HTTP 码。
- [ ] 删除完成页只有报告和「完成」按钮，不再出现重复的「导出删除记录」。
- [ ] 重复删除同一批（若手上还有已删 id）：结果记为"跳过"而非"失败"，不报错。
- [ ] 删除完成后重开弹窗（或点胶囊回来）：已删消息不会从缓存/会话里"复活"。

## 5. 设置页

- [ ] 四个标签（AI 平台 / 审查策略 / 行为 / 诊断）都能渲染，切换无报错。
- [ ] AI 平台：切换平台、填 Key、拉取模型列表、模型下拉（在弹窗底部时向上展开）。
- [ ] 审查策略：新建/重命名/删除策略、编辑正文、切换当前策略，重启 BD 后仍在。
- [ ] 行为页：并发、节流、单次上限、备份模式、包含编辑过的消息——改完重启 BD 后仍在。
- [ ] 旧版 `review.policyPrompt` 迁移：v0.2.0 老配置升上来后，策略库里出现"已迁移策略"并被选中。

## 6. 收尾

- [ ] 禁用插件：按钮消失、悬浮胶囊消失、样式清除、无残留监听（控制台无报错）。
- [ ] 重新启用 / 热重载：一切恢复正常，不出现重复按钮。
- [ ] 全程控制台无本插件的未捕获异常。

## 已发布版本的实测记录

| 版本 | 扫描 | AI 审查 | 删除 | 备注 |
|---|---|---|---|---|
| v0.6.3 | 已测 | 已测（525 条/15 命中） | 未测 | 首个公开发布 |
| v0.6.5 | 已测 | 已测 | 未测 | 胶囊锚定修复 |
| v0.6.6 | 待测 | 待测 | **基础删除已测** | 用户于 2026-08-21 确认可正常删除；安全性三连与异常路径仍待完整走查。**导出（备份/删除记录）在用户环境不可用，已知问题待修** |
| v0.6.7 | 待测 | 待测 | **基础删除 + 导出已测** | 用户于 2026-08-21 确认系统保存框与“保存成功后再删除”流程可用；自动化覆盖 MD/TXT/JSON、取消与失败路径；移除删除后重复导出 |
